<%= provide :title, "create Course" %>
<div class="page-header">
  <h1><%= "Course" %></h1>
</div>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for [:trainner, @course], html: {id: :form_id } do |f| %>
    <%= render "shared/error_messages", object: @course %>
      <% render "course_form", f: f %>
    <% end %>
  </div>
</div>
