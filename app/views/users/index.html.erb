<% provide :title, t(".allusers") %>
<h1><%= t ".allusers" %></h1>
<%= will_paginate %>

<% if admin? current_user %>
<%= link_to t(".addusers"), signup_path, class: "btn btn-warning", id: "new-user" %>
<% end %>
<% if trainner?(current_user)|| admin?(current_user)%>
<%= link_to "new course", new_trainner_course_path, class: "btn btn-warning", id: "new-course" %>
<% end %>
<ul class="users">
  <%= render @users %>
</ul>

<%= will_paginate %>
